# Gateway API

Installs a pinned upstream Gateway API CRD bundle with server-side apply. The module owns only API definitions; Gateway instances and policies belong to consumers.

Inputs: `release_version`, `channel`, and `force_conflicts`.
