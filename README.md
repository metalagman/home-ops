# home-ops

Reusable, public-safe Terraform modules for Kubernetes APIs and operators.
This repository does not own a live environment or Terraform state.

See [terraform/modules/README.md](terraform/modules/README.md) for the module
catalog and run `task modules:verify` to validate it.
